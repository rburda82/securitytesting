extraction:
  csharp:
     after-prepare:
        - export 
          PATH=$LGTM_WORKSPACE/tools:$PATH
     index:
        all_solutions: true
        build_command:
        - msbuild ./ConsoleApp5/ConsoleApp5.sln